# Representative fixtures

Entirely fictional: one customer, one transaction, Asteria Digital and its profile, one case with evidence, a proposal, policy and human review records, normalized messages, and a structured report. This is schema coverage, not the full demo dataset or evidence of a real merchant risk. Dates and IDs are deterministic. Profile risk data is illustrative synthetic data.

Run `npm test` to validate shapes and linked references. QA/demo workers will expand and load the dataset later.
